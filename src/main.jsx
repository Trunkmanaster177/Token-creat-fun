import React from "react";
import ReactDOM from "react-dom/client";
import CreateTokenApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CreateTokenApp />
  </React.StrictMode>
);
